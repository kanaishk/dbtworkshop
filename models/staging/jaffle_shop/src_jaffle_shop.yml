version: 2

sources:
  - name: jaffle_shop
    database: dbtworkshop
    schema: jaffle_shop
    tables:
      - name: customers
      - name: orders
        loaded_at_field: etlloaded_at
        freshness: 
          error_after:
            count: 2
            period: day
          warn_after:
            count: 1
            period: day
